# Development environment variables (do NOT commit sensitive values)
# Copy this to backend/.env and fill in the values
DATABASE_URL="postgresql://postgres:PASSWORD@HOST:5432/postgres?sslmode=require"